# Test CRUD

nginx.conf in root

Run `sudo npm install -g parcel-bundler` to install parcel

Run `npm install` to install dependecies

Build **ReactJS**
```
cd reactjs
npm run build
```

Build **SvelteJS**
```
cd sveltejs
npm run build
```

Build **VueJS**
```
cd vuejs
npm run build
```
