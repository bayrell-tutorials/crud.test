# crud.test

nginx.conf in root

Run `npm install` to install dependecies

Build **ReactJS**
```
cd reactjs
npm run build
```

Build **SvelteJS**
```
cd sveltejs
npm run build
```

Build **VueJS**
```
cd vuejs
npm run build
```
