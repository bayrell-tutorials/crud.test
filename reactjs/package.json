{
  "name": "hello-world",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "parcel build src/index.html --public-url /reactjs --out-dir ../web/reactjs --cache-dir var/parcel",
    "watch": "parcel watch src/index.html --public-url /reactjs --out-dir ../web/reactjs --cache-dir var/parcel"
  }
}
