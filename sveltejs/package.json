{
  "name": "hello-world",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "parcel build src/index.html --public-url /sveltejs --out-dir ../web/sveltejs --cache-dir var/parcel",
    "watch": "parcel watch src/index.html --public-url /sveltejs --out-dir ../web/sveltejs --cache-dir var/parcel"
  }
}
